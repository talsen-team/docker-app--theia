version: '3'

services:
  server--theia:
    build:
      context:      './../../docker/server--theia'
      dockerfile:   'default.docker'
    container_name: 'server--theia'
    image:          'talsenteam/docker-theia:v0.11.0'
    ports:
      -             '80:3000'
    restart:        'no'
